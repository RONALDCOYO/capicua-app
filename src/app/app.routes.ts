import { Routes } from '@angular/router';
import { CapicuaCheckerComponent } from './capicua-checker/capicua-checker.component';

export const routes: Routes = [
    {
        path: '',
        component: CapicuaCheckerComponent
    }
    
];
